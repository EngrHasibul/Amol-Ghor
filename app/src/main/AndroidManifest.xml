<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shohihadis">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_main_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_main_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SunnatActivity"></activity>
        <activity android:name=".HadisActivity" />
        <activity android:name=".RujaActivity" />
        <activity android:name=".TajbihActivity" />
        <activity android:name=".DuyaActivity" />
        <activity android:name=".NamajActivity" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>